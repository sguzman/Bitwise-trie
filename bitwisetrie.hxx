#pragma once

#include <iostream>

#include "binary.hxx"

namespace despairagus {
	namespace bitwisetrie {
		using std::cout;
		using std::ostream;

		template <typename A>
		class bitwisetrie {
			explicit bitwisetrie(void) = delete;
		};
	}
}